<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.diplom">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:screenOrientation="portrait">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN"
                    android:screenOrientation="portrait" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Admin_activity"
            android:label="@string/title_admin_activity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".ChangeTest"
            android:label="@string/title_ChangeTest" />

        <activity
            android:name=".RegForm"
            android:label="@string/title_RegForm" />

        <activity
            android:name=".TestCreating"
            android:label="@string/title_TestCreating"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".Info"
            android:label="Info" />

        <activity
            android:name=".TestSelection"
            android:label="TestSelection" />

        <activity
            android:name=".ResultsActivity"
            android:label="ResultsActivity" />

        <activity
            android:name=".TestActivity"
            android:label="TestActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>
</manifest>